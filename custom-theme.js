import { create } from '@storybook/theming/create';
import Logo from './img/storybook-logo.svg';

export default create({
    base: 'light',
    brandTitle: 'CustomTheme Style guide',
    brandUrl: '',
    brandImage: Logo,
});
